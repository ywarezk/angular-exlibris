import { ThrottlingDirective } from './throttling.directive';

describe('ThrottlingDirective', () => {
  it('should create an instance', () => {
    const directive = new ThrottlingDirective();
    expect(directive).toBeTruthy();
  });
});
